version: "2"
sql:
  - engine: "postgresql"
    queries: "../../schemas/postgres/queries"
    schema: "../../schemas/postgres/migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_prepared_queries: false
